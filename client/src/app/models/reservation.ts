export interface Reservation {
    id: number,
    firstName: string;
    lastName: string;
    address: string;
    postalCode: string;
    postalCity: string;
    phoneNumber: string;
    email: string;
}